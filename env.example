# ===========================================
# Starlight Network Monitor - Environment Variables
# ===========================================
# 
# NOTE: Most configuration is now managed through the web UI!
# After starting the application, access Settings from the dashboard
# to configure admin password, webhooks, and monitoring options.
#
# This file contains INFRASTRUCTURE settings only.
# You typically don't need to create a .env file - the defaults
# in docker-compose.yml work out of the box.
# ===========================================

# Backend Configuration
NODE_ENV=development
PORT=4000

# Database Configuration (PostgreSQL)
# Only change these if using an external database
DATABASE_URL=postgresql://starlight:starlight@localhost:5432/starlight_db
POSTGRES_USER=starlight
POSTGRES_PASSWORD=starlight
POSTGRES_DB=starlight_db

# MQTT Broker Configuration
# Only change these if using an external MQTT broker
MQTT_BROKER_URL=mqtt://localhost:1883
MQTT_USERNAME=
MQTT_PASSWORD=

# Frontend Configuration
# These are set automatically in docker-compose.yml
VITE_API_URL=http://localhost:4000
VITE_WS_URL=ws://localhost:4000

# ===========================================
# The following settings are managed via the Web UI:
# ===========================================
# - Admin Password (set on first-time setup)
# - n8n Webhook URL
# - n8n Webhook Secret  
# - Probe Timeout
# - Status History Retention
# - Internet Check Targets
#
# Access these settings by clicking the gear icon
# in the dashboard header after logging in.
# ===========================================
